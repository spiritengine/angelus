# SKEIN local state — registered globally via ~/.skein/projects.json
.skein/

# Python / Node / etc. — pre-populate as the project grows
__pycache__/
*.pyc
.venv/
node_modules/
*.egg-info/
.pytest_cache/

# Angelus runtime state
state/
observations/
findings/
dispatches.log

# Editor / OS noise
*~
*#
.#*
.DS_Store

# Patrick's scratch
topost
